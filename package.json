{
  "name": "hvt-write-api",
  "version": "1.0.0",
  "description": "A lambda to do write actions against the HVT dynamo db",
  "main": "index.js",
  "scripts": {
    "lint": "eslint '*/**/*.ts' --quiet --fix",
    "test": "jest --coverage tests/unit",
    "build:db:test": "docker run --name hvt-dynamo-test -p 8001:8000 -d amazon/dynamodb-local",
    "test:integration": "npm run build:db:test && DYNAMO_URL='http://localhost:8001' DYNAMO_REGION='local' jest --coverage tests/integration && npm run destroy:db:test",
    "destroy:db:test": "docker stop hvt-dynamo-test && docker rm hvt-dynamo-test",
    "build:dev": "NODE_ENV=development webpack-cli",
    "build:prod": "NODE_ENV=production webpack-cli",
    "start:dev": "sam local start-api -p 3001 --docker-network hvt-network"
  },
  "contributors": [
    {
      "name": "Shaun Hare",
      "email": "shaun.hare@dvsa.gov.uk"
    }
  ],
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.747.0",
    "dotenv": "^8.2.0",
    "joi": "^17.2.1"
  },
  "devDependencies": {
    "@dvsa/eslint-config-ts": "^2.0.0",
    "@types/aws-lambda": "^8.10.59",
    "@types/jest": "^26.0.13",
    "@types/joi": "^14.3.4",
    "@types/node": "^12.0.8",
    "@types/uuid": "^8.3.0",
    "aws-sam-webpack-plugin": "^0.6.0",
    "copy-webpack-plugin": "^6.2.1",
    "eslint": "^7.6.0",
    "eslint-import-resolver-typescript": "^2.3.0",
    "jest": "^26.4.2",
    "source-map-support": "^0.5.19",
    "ts-jest": "^26.3.0",
    "ts-loader": "^8.0.3",
    "typescript": "^3.9.7",
    "uuid": "^8.3.0",
    "webpack": "^4.44.1",
    "webpack-cli": "^3.3.12"
  },
  "engines": {
    "node": "^12.18.4"
  }
}
